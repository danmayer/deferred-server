#!/bin/bash
log='/tmp/init.log'
echo "begin" >> $log
apt-get update &>> $log
#apt-get install -y ruby ruby1.8-dev build-essential wget libruby-extras libruby1.8-extras git-core &>> $log
cd /tmp

#########
# at the moment it seems that the bitnami image I have been using ignores user_data, halting this path
# and moving forward with fog bootstrapping chef
#########

echo "end" >> $log
